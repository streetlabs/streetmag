<%= simple_form_for [:admin, @publication, :admin, @section] do |f| %>
  <% if @section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% @section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :is_static %>
    <%= f.association :parent, :collection => @publication.sections %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
