<% title "STREETMAG » Publications" %>  

<div class="admin">
  <h1>NEWS POSTS</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Author</th>
    <th>Date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= post.author %></td>
    <td><%= l post.created_at, :format => :short %></td>
    <td> <%= link_to 'Show', admin_publication_admin_post_path(@publication, post) %> </td>
    <td> <%= link_to 'Edit', edit_admin_publication_admin_post_path(@publication, post) %> </td>
    <td> <%= link_to 'Destroy', admin_publication_admin_post_path(@publication, post), :confirm => 'Are you sure?', :method => :delete %> </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_admin_publication_admin_post_path(@publication) %>
</div>